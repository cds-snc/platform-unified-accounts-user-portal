name: Integration Test Staging

concurrency:
  group: docker-update-staging
  cancel-in-progress: false

on:
  repository_dispatch:
    types:
      - integration-test-staging

permissions: {}

jobs:
  integration-test:
    permissions:
      id-token: write
      contents: read
    uses: ./.github/workflows/integration-test.yml
    with:
      account_id: ${{ vars.STAGING_AWS_ACCOUNT_ID }}
      bucket_name: ${{ vars.STAGING_INTEGRATION_TEST_BUCKET_NAME }}
      idp_url: ${{ vars.STAGING_INTEGRATION_TEST_IDP_URL }}
      portal_url: ${{ vars.STAGING_INTEGRATION_TEST_PORTAL_URL }}
    secrets:
      log_analytics_workspace_id: ${{ secrets.LOG_ANALYTICS_WORKSPACE_ID }}
      log_analytics_workspace_key: ${{ secrets.LOG_ANALYTICS_WORKSPACE_KEY }}
      register_email: ${{ secrets.STAGING_INTEGRATION_TEST_REGISTER_EMAIL }}
      username: ${{ secrets.STAGING_INTEGRATION_TEST_USERNAME }}
      password: ${{ secrets.STAGING_INTEGRATION_TEST_PASSWORD }}
      test_callback_url: ${{ secrets.STAGING_INTEGRATION_TEST_CALLBACK_URL }}
      test_client_id: ${{ secrets.STAGING_INTEGRATION_TEST_CLIENT_ID }}
      totp_secret: ${{ secrets.STAGING_INTEGRATION_TEST_TOTP_SECRET }}
      zitadel_service_account_key: ${{ secrets.STAGING_INTEGRATION_TEST_ZITADEL_SERVICE_ACCOUNT_KEY }}

  workflow-failure:
    needs:
      - integration-test
    if: failure()
    permissions: {}
    uses: ./.github/workflows/workflow-failure.yml
    secrets:
      LOG_ANALYTICS_WORKSPACE_ID: ${{ secrets.LOG_ANALYTICS_WORKSPACE_ID }}
      LOG_ANALYTICS_WORKSPACE_KEY: ${{ secrets.LOG_ANALYTICS_WORKSPACE_KEY }}
      SLACK_WEBHOOK_URL: ${{ secrets.STAGING_CLOUDWATCH_ALERT_SLACK_WEBHOOK }}
